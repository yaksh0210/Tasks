---
- name: install apache and php
  apt:
    name: "{{ item }}"
    state: present
  loop:
    - php
    - apache2
    - libapache2-mod-php

- name: apache is running
  service:
    name: apache2
    state: started

